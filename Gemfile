# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "minitest", "~> 5.0"
gem "rake", "~> 13.0"

# The typed_params template parses times in a named zone, so its tests need real zones.
# Runtime takes no dependency on this: the gem itself needs only rubocop.
gem "activesupport"
