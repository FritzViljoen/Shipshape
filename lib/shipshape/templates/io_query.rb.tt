# frozen_string_literal: true

# Generated by shipshape. This file is yours now.
#
# **A read of something outside this process** — an exchange rate fetched, a customer looked
# up in somebody else's system. It answers with a shape or an array of them, so the shape of
# what comes back is decided here rather than leaking whatever the far end happened to send.
#
# It is a **sister of Query**, not a subclass of one: every class inherits exactly one base
# and no deeper, so the contract is repeated here rather than inherited.
#
# **Why it is a separate kind, and the reason is the same as its sibling's.** A query is one
# read of this application's own store, cheap and local and never failing for reasons
# outside the building. An external read is a network round trip that can be slow, can time
# out, and can be down — and folding it into a query would hide all of that behind a call
# that reads like a database lookup. It also cannot happen inside a command, because a
# command is one transaction and this would hold it open across the wire.
#
# There is no Result here, exactly as there is none on a query: finding nothing is an
# **answer**. A far end that is unreachable is different — that is a failure, and it raises.
class IoQuery
  include TypedArguments

<%- if auth -%>
  extend Permission
<%- end -%>

  # A query has no Result to carry a refusal, and wrapping one would make every caller
  # unwrap a value that was never in doubt. So it raises, like every other query failure.
  #
  # An operation implementing `anonymous_call` instead of `call` runs before anyone is
  # identified and is not checked. That is a property of the class, never of the caller.
<%- if auth -%>
  def self.call(actor: nil, **arguments)
    raise Permission::Refused, permission unless permits?(actor)

    operation = anonymous? ? new(**arguments) : new(actor: actor, **arguments)
    # `send`, because the entry point is private: an operation's whole public surface is
    # this class method, so `call` and `anonymous_call` are implementation. A literal
    # symbol is not the generated dispatch `code-is-written-not-generated` forbids — it
    # names one of two methods, and both are greppable.
    answer = operation.send(anonymous? ? :anonymous_call : :call)
<%- else -%>
  def self.call(**arguments)
    answer = new(**arguments).send(:call)
<%- end -%>

    raise TypeError, "#{name}#call must answer with shapes, got #{answer.class}" unless shapes?(answer)

    answer
  end

  def self.shapes?(answer)
    return answer.all? { |entry| entry.is_a?(Shape) } if answer.is_a?(Array)

    answer.is_a?(Shape)
  end
  private_class_method :shapes?
end
