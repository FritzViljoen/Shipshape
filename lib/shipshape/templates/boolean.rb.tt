# frozen_string_literal: true

# Generated by shipshape. This file is yours now.
#
# A name for "true or false", so `typed(tie, Boolean)` reads like every other assertion.
#
# Ruby has none, and the usual workaround reopens TrueClass and FalseClass to include a marker
# module. That changes two objects nobody owns, invisibly. This is a name and nothing else:
# included nowhere, and TypedArguments knows it by identity.
module Boolean
  def self.to_s
    "Boolean"
  end

  def self.inspect
    "Boolean"
  end
end
