# frozen_string_literal: true

# Generated by shipshape. This file is yours now.
#
# **A view component is the other presentation kind.** The call matrix gives it one row —
# `shape` — because it renders what it was handed and reaches for nothing. A component that
# can reach an operation is a controller with a template.
#
# It is held to the shape's rules for the same reason a shape is: `Current.user` read at
# render time, a clock read in a template, or a record arriving through an argument are the
# same defects wherever the rendering happens.
#
# **Installed only on request** — `shipshape install --view-components` — because this
# inherits from the `view_component` gem, and writing it into an application that does not
# have that gem stops the boot. Nothing else here can fail that way.
class ApplicationViewComponent < ViewComponent::Base
  # A component's initializer asserts what it was handed, exactly as every other kind does —
  # and `typed` is where a record arriving as an argument is refused by name.
  include TypedArguments

  # The same refusal the generated `Shape` carries, and deliberately the same code: one rule
  # about the presentation layer, stated once. A component holding a record renders a
  # template that queries — the N+1 nobody can find, because the call that causes it is in
  # an `.erb` file and names nothing.
  extend HoldsNoRecords
end
